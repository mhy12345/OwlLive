each it in mlist
	- if (it.postTime.getTime() > last_time)
		hr
		div.message #{it.text}
		div.post-user
			span.like-tag(onclick="sendLike('#{it._id}','#{user}')") [+#{it.like}] 
			span posted by #{it.user}
			span.post-time #{it.postTime.getTime()}
